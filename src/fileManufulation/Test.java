package fileManufulation;

import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.HashSet;

public class Test {

    public static void main(String[] args) {

        String[] cars1 = {"Volvo", "BMW", "Ford", "Mazda"};

        ArrayList<String> cars = new ArrayList<String>();
        cars.add("Volvo");
        cars.add("Honda");

        HashMap<K, V>;

        HashSet<E>

        ArrayList<Object> - Unordered list of objects that allows duplicates
        ArrayList<Object> array = new ArrayList<>();
        array.add(object1);
        array.get(index);
        array.remove(object);

        HashSet<Student> - Unordered set of Student objects, there are no duplicates
        To override the default hashCode();

        TreeSet<Student> - Ordered set of Student objects, no duplicates
        compareTo() method checks for duplicates

        Example:
            implements Comparable<Movie>{
            @Override
            public int compareTo(Movie otherMovie) {
                return this.getTitle().compareTo(otherMovie.getTitle());

                HashMap<Student, Boat> - Unordered mapping of Student objects to Boat objects
                TreeMap<Student, Boat> - Ordered mapping of Student objects to Boat objects



    }

}
